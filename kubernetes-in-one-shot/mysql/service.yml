kind: Service
apiVersion: v1
metadata:
  name: mysql-svc
  namespace: mysql
spec:
  clusterIP: None    #Headless service means not to be exposed to outside world like we do using portforward
  selector:
    app: mysql
  ports:
  - name: mysql
    protocol: TCP
    port: 3306
    targetPort: 3306
